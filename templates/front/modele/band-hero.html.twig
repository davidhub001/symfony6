<div class="hero-section mt-5 mb-5" style="background: url('{{ asset('assets/images/') }}{{image}}') no-repeat center center/cover;">
    <div class="content band-text-ombre">
        <h1>{{titre}}</h1>
        <p>{{description}}</p>
        {% if bouton %}
            <a href="#" class="btn-custom">Voir</a>
        {% endif %}
    </div>
</div>